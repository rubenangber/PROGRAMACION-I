#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char const *argv[]) {
    char nombre[] = "Rubén", temp, final;
    int i, j;
    int lenth = strlen(nombre);

    j = lenth - 1;

    printf("%d", lenth);

    for (i = 0; i < lenth; i++) {
        temp = nombre[i];
        
    }

    return 0;
}
